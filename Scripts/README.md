# Scripts

`00_config.R` contains central user-editable analysis and plotting settings.

`00_validate_inputs.sh` is the first canonical repository-aware script.

`legacy_current/` contains the currently tested enrichment/analysis scripts from development. They are retained for provenance and will be refactored one by one to read immutable `Sheets/` inputs and write numbered `Data/Intermediate/` outputs.
